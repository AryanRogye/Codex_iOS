# Provider: codex (default) or openai
RELAY_PROVIDER=codex

# Optional auth for relay endpoints
RELAY_AUTH_TOKEN=change-me

# Optional defaults
RELAY_DEFAULT_MODEL=
RELAY_CODEX_BIN=codex
RELAY_DATA_PATH=

# Required only when RELAY_PROVIDER=openai
OPENAI_API_KEY=
